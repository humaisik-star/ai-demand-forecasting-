joblib==1.5.3
matplotlib==3.11.0
numpy==2.5.0
openai==2.44.0
optuna==4.9.0
pandas==3.0.3
python-dotenv==1.2.2
scikit-learn==1.9.0
seaborn==0.13.2
xgboost==3.3.0

# Optional: SHAP explainability (explain.py only). Pulls numba, which has no
# wheel for this numpy/Python combo, so it is kept out of the pinned set and
# out of CI. Install separately when you want SHAP plots:  pip install shap

